# EasyR1 接入、attention mask 修复与 one-step GRPO 调试记录

日期：2026-08-08  
工作分支：`codex/latent-step-reward`  
Git 状态：未创建 commit，未推送远程；既有未提交文件未暂存、未修改或提交。

## 1. 本轮目标

将 OneThinker 的 EasyR1 固定版本以 vendored 方式接入 CoLT，并使其能够：

- 使用 CoLT 的 `latent_reasoning_generate` 完成 rollout；
- 对 hidden latent reasoning 条件下的可见 response 计算策略 log-prob；
- 使用 CoLT 兼容的 outcome reward；
- 在固定参数时保证 rollout old log-prob 与 actor 重算 log-prob 一致；
- 在独立 `colt-easyr1` Conda 环境中完成真实 one-step GRPO。

## 2. 主要改动

### 2.1 EasyR1 本地化与训练入口

- 固定上游 EasyR1 提交 `4a36ad286d04382fce9816ac4429e650157a5f11` 导入为仓库内 `EasyR1/`。
- 脚本默认 EasyR1 路径改为 `$REPO_ROOT/EasyR1`，不再依赖外部 OneThinker 工作区。
- 新增 CoLT 固定 v2 的 GRPO 配置、预检、RL 命令入口与环境依赖清单。

### 2.2 CoLT rollout 与 reward

- 新增 `CoLTTransformersRollout`，直接调用 CoLT 的 `latent_reasoning_generate`，不经 vLLM。
- rollout 生成完成后，对完整 batch 进行 teacher-forced response 打分，写入 `rollout_log_probs`；该路径与 actor 的 latent-conditioned response log-prob 使用同一语义。
- 增加 CoLT-compatible outcome reward：支持 bare answer 与 `<answer>...</answer>`，并将最终 reward 写到有效 response 的最后一个 token。
- latent 模式 prompt 不再要求模型输出可见 `<think>`；visible 模式仍保留原行为。

### 2.3 attention mask 与 left-padding 修复

根因：EasyR1 对 prompt 做 left-padding；CoLT 在带 KV cache 的 latent transition / response decode 中曾只传入当前 token 的 mask，导致历史 padding 不再被屏蔽。rollout 和 actor 因此可能看到不同上下文。

修复：

- 在 Qwen3-VL CoLT 实现中加入 cached attention mask 扩展辅助函数：保留完整 prompt mask，再为 latent cache 与当前 response token 追加有效位。
- 应用于 latent reasoning forward、latent-conditioned response forward 与 `latent_reasoning_generate`。
- rollout 保留原始 left-padded prompt/attention mask，不再去 padding；batch scorer 的 mRoPE position 维度与 actor 完全对齐。
- `latent_to_decoder_scale`、`alpha` 改为一维参数，绕开 FSDP 对 0-D parameter 的限制；加载时自动把 fixed-v2 checkpoint 的旧标量权重 reshape 为一维。

## 3. 调试过程与结论

| 现象 | 根因 | 处理 |
| --- | --- | --- |
| left-padding 下 rollout 与 actor log-prob 可能偏离 | cached forward 丢失完整历史 attention mask | 完整 mask 传播 + left-padding 回归测试 |
| FSDP 初始化失败 | CoLT checkpoint 中两个可训练标量为 0-D tensor | 参数改为 `[1]`，兼容加载旧 checkpoint |
| reward 运行时报 `pylatexenc` 缺失 | MathRuler grader 的实际运行依赖未在最小环境中安装 | 加入 `requirements-colt.txt` 与 preflight 检查 |
| one-step 在系统盘 `/tmp` 停在结束阶段 | EasyR1 即使 `save_freq=-1` 仍会保存最终完整 checkpoint；优化器分片约 39 GB，系统盘 I/O 饱和 | 改用 NVMe 临时目录并启用 `save_model_only=true`；训练链路本身已完成 |

## 4. 验证结果

### 一致性与单元测试

- fixed-v2 文本 left-padding（32 tokens）：`max_abs_diff = 0.0`。
- fixed-v2 Chart2Text 图文 left-padding（32 tokens）：`max_abs_diff = 0.0`。
- fixed-v2 两条异长文本 batch + left-padding：`max_abs_diff = 0.0`。
- 2-rank FSDP Transformers 参数同步检查：通过。
- Python 编译、`git diff --check`、48 项单元/脚本回归测试：全部通过。

### one-step GRPO

- 环境：`/data/nvme0/lkl/conda/envs/colt-easyr1`。
- 设置：2 张 A100，2 条临时文本样本，`n=2`，`max_steps=1`，fixed-v2 checkpoint。
- 结果：rollout、old log-prob、actor update、Step 1、final validation、双 rank 模型 checkpoint 均完成；无 traceback。
- 产物：`/data/nvme0/lkl/colt-easyr1-smoke.6Vmhpq/one_step.log`，checkpoint tracker 记录 `last_global_step: 1`。
- 临时样本的 `reward_score=0.9` 仅证明训练路径可运行，不表示模型效果。

## 5. 环境边界与后续注意事项

- preflight 在 `colt-easyr1` 中为 `ready=true`，运行时缺失包为空。
- `pip check` 仍提示 EasyR1 包元数据声明的 `vllm`、`liger-kernel` 未安装；当前 CoLT 配置使用自定义 Transformers rollout，实际 one-step GRPO 已验证不依赖它们。后续若切换 vLLM 或 Liger 路径，应单独安装并重新做兼容性测试。
- 临时测试目录保留，未主动删除：NVMe 的 `/data/nvme0/lkl/colt-easyr1-smoke.6Vmhpq`，以及此前系统盘上的 `/tmp/colt-easyr1-smoke.IFTbDS`。后者含完整优化器 checkpoint，占用空间较大；清理前应确认不再需要其日志和产物。

## 6. 关键文件

- `transformers-4.57.0/src/transformers/models/qwen3_vl/modeling_qwen3_vl.py`
- `EasyR1/verl/workers/rollout/colt_transformers_rollout.py`
- `EasyR1/verl/reward_function/colt_outcome.py`
- `scripts/lkl_8gpu/easyr1/check_logprob_parity.py`
- `scripts/lkl_8gpu/easyr1/check_fsdp_transformers_sync.py`
- `scripts/lkl_8gpu/easyr1/preflight.py`
- `tests/unit/test_colt_easyr1_contract.py`

## 7. image-only Outcome-GRPO 与当前状态（2026-08-09）

### 已完成

- 训练数据改为官方 `onethinker_rl_train.json` 的 image-only manifest：仅筛除不可访问媒体，保留 `189,645` 条样本；图像根目录指向已有 `CoLT_Train_Dataset`。
- outcome-only GRPO 的数据、prompt、reward、rollout、checkpoint 和脚本入口已连通；此前 2-GPU、1-step 临时文本验证证明一次完整更新能够结束。
- 增加 rollout/actor old log-prob parity gate：比较同一条已采样轨迹（相同 token、mask、position 与图像输入）的有效 response token log-prob；更新前不允许进入 actor update。
- 修复 rollout 模型错误以 FP32 加载的问题：改用 Transformers 的 `torch_dtype`，并显式转换到配置的 BF16；补充对应回归测试。

### 当前阻塞：前向 parity 尚未通过

- 目标阈值：`max_abs_diff <= 0.05`。
- 修复前，8 卡 image smoke 的差异为 `0.366386`；诊断中一度观察到 `2.10813`。
- 修复 rollout 精度后，最新 8 卡诊断降至 `0.670174`，说明 FP32/BF16 不一致是一个真实问题，但仍远超阈值。
- 已验证 actor 参数同步到 rollout 后参数最大差异为 `0.0`；FSDP actor 与其 unwrapped 前向约为 `2.38e-6`。剩余差异位于 FSDP actor 与普通 Transformers rollout 的前向/打分路径，根因尚未确认。
- 因此：**当前仅完成基础链路和一次受控更新验证，尚不能启动正式 image-only RL，也不应运行计划中的 8 卡 2-step smoke。**

### 精度结论与下一步

- parity 不比较优化器状态；它发生在 `optimizer.step()` 之前，优化器状态不能解释当前冻结策略的前向差异。
- actor 与 rollout 的参数前向精度应保持一致（当前均为 BF16）；将 rollout 改为 FP32 会扩大而非解决 parity 风险。
- 当前 `adamw_bf16` 的动量、二阶矩和 Kahan buffer 默认均为 BF16。将优化器状态改为 FP32 可作为后续 RL 数值稳定性实验，但不能作为 parity 修复；应在 parity 通过后单独做短更新对照。
- 下一步按最小诊断继续隔离 FSDP 与普通 Transformers 的 latent-conditioned log-prob 前向差异；仅在 parity gate 通过后，执行真实 8 卡 2-step smoke，再由用户决定全量训练。
